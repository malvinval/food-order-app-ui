import { useState } from "react";
import './index.css';
import Navbar from "./components/ui/Navbar";

const App = () => {
  const element = (
    <div id="container">
      <Navbar />
      
    </div>
  );

  return element;
}

export default App;
